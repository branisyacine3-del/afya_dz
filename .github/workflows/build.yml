name: Dart-Only Config Build

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Clean Build Setup
      run: |
        # 1. تنظيف وإنشاء المشروع من جديد
        rm -rf android
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # 2. 🔥 التحديث السحري: إجبار المشروع على استخدام Kotlin 1.9.0 لتتوافق مع المكتبات
        sed -i 's/ext.kotlin_version = .*/ext.kotlin_version = "1.9.0"/' android/build.gradle
        
        # 3. إضافة أذونات الإنترنت والموقع والكاميرا
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <uses-permission android:name="android.permission.CAMERA"\/>\n    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml
        
        # 4. رفع الحد الأدنى للإصدار (MinSDK) ليعمل مع المكتبات الحديثة
        sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 23/g' android/app/build.gradle

    - name: Get dependencies
      run: flutter pub get

    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-app-final
        path: build/app/outputs/flutter-apk/app-release.apk
