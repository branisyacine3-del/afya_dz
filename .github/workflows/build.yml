name: Fix Android V2 Embedding

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. جلب الملفات
    - uses: actions/checkout@v4

    # 2. إعداد الجافا
    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    # 3. إعداد فلاتر
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    # 4. 🔥 الخطوة الحاسمة: إعادة بناء مجلد أندرويد ليتوافق مع V2
    - name: Recreate Android Folder (Fix V2 Error)
      run: |
        # نحفظ المفتاح جانباً
        mv android/app/google-services.json .
        
        # نحذف مجلد أندرويد القديم (سبب المشكلة)
        rm -rf android
        
        # ننشئ مجلد أندرويد جديد وحديث
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # نعيد المفتاح لمكانه
        mv google-services.json android/app/

    # 5. إضافة إذن الإنترنت الذي يختفي عند إعادة البناء
    - name: Inject Internet Permission
      run: |
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml

    # 6. تحميل المكتبات
    - name: Get dependencies
      run: flutter pub get

    # 7. بناء التطبيق
    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    # 8. حفظ التطبيق الجديد
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-fixed-v2
        path: build/app/outputs/flutter-apk/app-release.apk
