name: Guaranteed Fix

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Repair & Inject (The Safe Way)
      run: |
        # 1. 🕵️‍♂️ سحب المفتاح من مكانه الحالي (حسب صورتك src/main)
        if [ -f android/app/src/main/google-services.json ]; then
          echo "✅ Found key in src/main"
          mv android/app/src/main/google-services.json .
        elif [ -f android/app/google-services.json ]; then
          echo "✅ Found key in app root"
          mv android/app/google-services.json .
        else
          echo "⚠️ Key not found, build might fail later"
        fi
        
        # 2. 🧹 إعادة بناء الأندرويد من الصفر
        rm -rf android
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # 3. 🔙 وضع المفتاح في المكان الصحيح
        mv google-services.json android/app/
        
        # 4. 🔥 الحقن في الملف الرئيسي (settings.gradle)
        # نبحث عن القوس plugins { ونضع السطر تحته مباشرة
        sed -i 's/plugins {/plugins {\n    id "com.google.gms.google-services" version "4.4.1" apply false/g' android/settings.gradle
        
        # 5. 🔥 الحقن في ملف التطبيق (app/build.gradle)
        # نبحث عن القوس plugins { ونضع السطر تحته مباشرة
        sed -i 's/plugins {/plugins {\n    id "com.google.gms.google-services"/g' android/app/build.gradle
        
        # 6. ✅ إضافة إذن الإنترنت
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml
        
        # 7. ⚙️ ضبط الحد الأدنى للإصدار
        sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 23/g' android/app/build.gradle

    - name: Get dependencies
      run: flutter pub get

    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-guaranteed-fix
        path: build/app/outputs/flutter-apk/app-release.apk
