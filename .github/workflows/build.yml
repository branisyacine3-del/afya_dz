name: Dart-Only Config Build

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Clean Build Setup & Fix Kotlin
      run: |
        # 1. تنظيف وإنشاء المشروع بطريقة Flutter الرسمية
        rm -rf android
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # 2. 🔥 التعديل الجراحي: البحث عن نسخة Kotlin القديمة واستبدالها بـ 1.9.0
        # هذا الأمر يبحث داخل settings.gradle الذي أنشأه فلاتر ويقوم بالتحديث
        sed -i 's/id "org.jetbrains.kotlin.android" version ".*"/id "org.jetbrains.kotlin.android" version "1.9.0"/' android/settings.gradle

        # 3. تحديث MinSDK
        sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 23/g' android/app/build.gradle

        # 4. إضافة الأذونات المطلوبة (الإنترنت، الموقع، الكاميرا)
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <uses-permission android:name="android.permission.CAMERA"\/>\n    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml

    - name: Get dependencies
      run: flutter pub get

    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-app-final
        path: build/app/outputs/flutter-apk/app-release.apk
 
