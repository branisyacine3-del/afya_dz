name: Modern Firebase Fix

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Repair & Link (Modern Way)
      run: |
        # 1. 🕵️‍♂️ البحث عن المفتاح وسحبه من أي مكان محتمل
        if [ -f android/app/src/main/google-services.json ]; then
          echo "Found key in src/main, moving it..."
          mv android/app/src/main/google-services.json .
        elif [ -f android/app/google-services.json ]; then
          echo "Found key in app root, moving it..."
          mv android/app/google-services.json .
        else
          echo "⚠️ Warning: Key not found in expected places!"
        fi
        
        # 2. 🧹 تنظيف وإعادة بناء الأندرويد
        rm -rf android
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # 3. 🔙 إعادة المفتاح للمكان القياسي
        mv google-services.json android/app/
        
        # 4. 🔥 الحقن الحديث (settings.gradle) - هذا هو حل المشكلة
        # نبحث عن بلوجن فلاتر ونضع بلوجن جوجل قبله
        sed -i 's/id "dev.flutter.flutter-gradle-plugin"/id "com.google.gms.google-services" version "4.4.1" apply false\n    id "dev.flutter.flutter-gradle-plugin"/g' android/settings.gradle
        
        # 5. 🔥 تفعيل البلوجن في التطبيق (app/build.gradle)
        # أيضاً نضعه بجانب بلوجن فلاتر لضمان الترتيب الصحيح
        sed -i 's/id "dev.flutter.flutter-gradle-plugin"/id "com.google.gms.google-services"\n    id "dev.flutter.flutter-gradle-plugin"/g' android/app/build.gradle
        
        # 6. ✅ إضافة إذن الإنترنت
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml
        
        # 7. ⚙️ ضبط الحد الأدنى للإصدار
        sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 23/g' android/app/build.gradle

    - name: Get dependencies
      run: flutter pub get

    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-modern-fix
        path: build/app/outputs/flutter-apk/app-release.apk
 
