name: Classic Fix (Guaranteed)

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Nuclear Repair & Link
      run: |
        # 1. نحفظ المفتاح
        mv android/app/google-services.json .
        
        # 2. نحذف ونعيد إنشاء الأندرويد
        rm -rf android
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # 3. نعيد المفتاح
        mv google-services.json android/app/
        
        # 4. 🔥 الحقن الكلاسيكي (الأكثر ضماناً)
        # نضيف الـ Classpath في ملف المشروع الرئيسي
        # نبحث عن كلمة dependencies ونضيف تحتها السطر مباشرة
        sed -i 's/dependencies {/dependencies {\n        classpath "com.google.gms:google-services:4.4.1"/g' android/build.gradle
        
        # 5. 🔥 تفعيل البلوجن في ملف التطبيق
        # نضيف سطر التفعيل في آخر الملف (هذا دائماً يعمل)
        echo "apply plugin: 'com.google.gms.google-services'" >> android/app/build.gradle
        
        # 6. ✅ إضافة إذن الإنترنت
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml
        
        # 7. ✅ ضبط الحد الأدنى للإصدار
        sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 23/g' android/app/build.gradle

    - name: Get dependencies
      run: flutter pub get

    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-classic-works
        path: build/app/outputs/flutter-apk/app-release.apk
