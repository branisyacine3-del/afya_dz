name: Full Rewrite Build

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Recreate & Overwrite Configs
      run: |
        # 1. البحث عن المفتاح وحفظه
        if [ -f android/app/src/main/google-services.json ]; then
          mv android/app/src/main/google-services.json .
        elif [ -f android/app/google-services.json ]; then
          mv android/app/google-services.json .
        fi
        
        # 2. إنشاء المشروع من جديد
        rm -rf android
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # 3. إعادة المفتاح للمكان الصحيح
        mv google-services.json android/app/
        
        # 4. 🔥 كتابة ملف settings.gradle يدوياً (الحل الأكيد)
        # هذا يضمن وجود بلوجن جوجل بدون أخطاء
        cat > android/settings.gradle <<EOF
        pluginManagement {
            def flutterSdkPath = {
                def properties = new Properties()
                file("local.properties").withInputStream { properties.load(it) }
                def flutterSdkPath = properties.getProperty("flutter.sdk")
                assert flutterSdkPath != null, "flutter.sdk not set in local.properties"
                return flutterSdkPath
            }
            settings.ext.flutterSdkPath = flutterSdkPath()
            includeBuild("\${settings.ext.flutterSdkPath}/packages/flutter_tools/gradle")
            repositories {
                google()
                mavenCentral()
                gradlePluginPortal()
            }
            plugins {
                id "dev.flutter.flutter-gradle-plugin" version "1.0.0" apply false
                id "com.google.gms.google-services" version "4.4.1" apply false
            }
        }
        include ":app"
        EOF
        
        # 5. 🔥 إضافة التفعيل في ملف التطبيق
        # نضيف السطر في نهاية الملف (أسهل وأضمن طريقة)
        echo "apply plugin: 'com.google.gms.google-services'" >> android/app/build.gradle
        
        # 6. إضافة إذن الإنترنت
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml
        
        # 7. رفع الإصدار
        sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 23/g' android/app/build.gradle

    - name: Get dependencies
      run: flutter pub get

    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-diagnostic-v1
        path: build/app/outputs/flutter-apk/app-release.apk
