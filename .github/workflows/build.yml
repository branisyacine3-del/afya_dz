name: Ultimate Fix

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Repair Android & Fix Plugins
      run: |
        # 1. حفظ المفتاح
        mv android/app/google-services.json .
        
        # 2. حذف المجلد القديم وإعادة الإنشاء
        rm -rf android
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # 3. استعادة المفتاح
        mv google-services.json android/app/
        
        # 4. 🔥 الحل السحري: إضافة تعريف فايربيز في المكان الصحيح (settings.gradle)
        # هذا الأمر يضيف مكتبة خدمات جوجل إلى قائمة الإضافات
        sed -i 's/plugins {/plugins {\n    id "com.google.gms.google-services" version "4.4.0" apply false/g' android/settings.gradle
        
        # 5. تفعيل الإضافة في ملف التطبيق
        echo "plugins { id 'com.google.gms.google-services' }" >> android/app/build.gradle
        
        # 6. إضافة إذن الإنترنت
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml
        
        # 7. رفع الحد الأدنى للإصدار (MinSDK) ليقبل فايربيز
        sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 23/g' android/app/build.gradle

    - name: Get dependencies
      run: flutter pub get

    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-ultimate-fix
        path: build/app/outputs/flutter-apk/app-release.apk
 
