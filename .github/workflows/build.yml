name: Full Repair Build

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. جلب الملفات
    - uses: actions/checkout@v4

    # 2. إعداد الجافا
    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    # 3. إعداد فلاتر
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    # 4. 🔥 العملية الجراحية: تجديد ملفات الأندرويد وإصلاح خطأ V2
    - name: Recreate Android Project & Fix Permissions
      run: |
        # 1. نحتفظ بالمفتاح
        mv android/app/google-services.json .
        
        # 2. نحذف المجلد القديم
        rm -rf android
        
        # 3. ننشئ مجلداً جديداً حديثاً
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # 4. نعيد المفتاح
        mv google-services.json android/app/
        
        # 5. 🔥 أهم خطوة: إضافة إذن الإنترنت للملف الجديد
        # هذا الأمر يبحث عن وسم التطبيق ويضيف قبله إذن الإنترنت
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml

    # 5. تحميل المكتبات
    - name: Get dependencies
      run: flutter pub get

    # 6. بناء التطبيق
    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    # 7. الرفع
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-final-fix
        path: build/app/outputs/flutter-apk/app-release.apk
 
