name: Fixed Plugin Location

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Repair Android & Inject Plugins Correctly
      run: |
        # 1. حفظ المفتاح
        mv android/app/google-services.json .
        
        # 2. تجديد مجلد أندرويد
        rm -rf android
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # 3. استعادة المفتاح
        mv google-services.json android/app/
        
        # 4. ✅ تعريف الفايربيز في الملف الرئيسي (settings.gradle)
        # نضعه داخل قوس plugins { الموجود أصلاً
        sed -i 's/plugins {/plugins {\n    id "com.google.gms.google-services" version "4.4.0" apply false/g' android/settings.gradle
        
        # 5. ✅ تفعيل الفايربيز في ملف التطبيق (app/build.gradle)
        # 🔥 التصحيح: نضعه في القمة داخل قوس plugins { بدلاً من الأسفل
        sed -i 's/plugins {/plugins {\n    id "com.google.gms.google-services"/g' android/app/build.gradle
        
        # 6. إضافة إذن الإنترنت
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml
        
        # 7. تحديث إصدار SDK
        sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 23/g' android/app/build.gradle

    - name: Get dependencies
      run: flutter pub get

    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-success
        path: build/app/outputs/flutter-apk/app-release.apk
