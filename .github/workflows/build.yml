name: Force Legacy Fix

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Reset & Force Injection
      run: |
        # 1. 🕵️‍♂️ إنقاذ المفتاح
        if [ -f android/app/src/main/google-services.json ]; then
          echo "Key found in src/main"
          mv android/app/src/main/google-services.json .
        elif [ -f android/app/google-services.json ]; then
          echo "Key found in app root"
          mv android/app/google-services.json .
        else
          echo "⚠️ Warning: No key found!"
        fi
        
        # 2. 🧹 هدم وبناء
        rm -rf android
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # 3. 🔙 استرجاع المفتاح
        mv google-services.json android/app/
        
        # 4. 🔥 الزرع الإجباري (Force Injection)
        # نقوم بإنشاء ملف مؤقت يحتوي على تعريف فايربيز، ثم ندمجه مع الملف الأصلي
        # هذا يضمن أن يكون التعريف في السطر الأول ولا يتأثر بالتحديثات
        echo 'buildscript { repositories { google(); mavenCentral() } dependencies { classpath "com.google.gms:google-services:4.4.1" } }' > android/temp_build.gradle
        cat android/build.gradle >> android/temp_build.gradle
        mv android/temp_build.gradle android/build.gradle
        
        # 5. 🔥 تفعيل البلوجن في التطبيق (أسهل طريقة)
        echo "apply plugin: 'com.google.gms.google-services'" >> android/app/build.gradle
        
        # 6. ✅ إضافة إذن الإنترنت
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml
        
        # 7. ⚙️ رفع الإصدار
        sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 23/g' android/app/build.gradle

    - name: Get dependencies
      run: flutter pub get

    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-force-fix
        path: build/app/outputs/flutter-apk/app-release.apk
 
