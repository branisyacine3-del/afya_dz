name: Full App Build

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Prepare & Configure
      run: |
        # 1. 🕵️‍♂️ إنقاذ ملف المفاتيح
        if [ -f android/app/src/main/google-services.json ]; then
          mv android/app/src/main/google-services.json .
        elif [ -f android/app/google-services.json ]; then
          mv android/app/google-services.json .
        fi
        
        # 2. 🧹 إنشاء مشروع جديد ونظيف (يضمن وجود الملفات الصحيحة)
        rm -rf android
        flutter create . --org com.yacine --project-name afya_dz --platform android
        
        # 3. 🔙 إعادة المفتاح
        mv google-services.json android/app/
        
        # 4. 🔥 الحقن الذكي في (settings.gradle)
        # نبحث عن السطر الخاص بفلاتر، ونضيف سطر فايربيز تحته مباشرة
        sed -i '/id "dev.flutter.flutter-gradle-plugin"/a \    id "com.google.gms.google-services" version "4.4.1" apply false' android/settings.gradle
        
        # 5. 🔥 الحقن الذكي في (app/build.gradle)
        # نفس الشيء، نضيف التفعيل تحت سطر فلاتر
        sed -i '/id "dev.flutter.flutter-gradle-plugin"/a \    id "com.google.gms.google-services"' android/app/build.gradle
        
        # 6. ✅ إضافة إذن الإنترنت
        sed -i 's/<application/<uses-permission android:name="android.permission.INTERNET"\/>\n    <application/g' android/app/src/main/AndroidManifest.xml
        
        # 7. ⚙️ رفع الإصدار
        sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 23/g' android/app/build.gradle

    - name: Get dependencies
      run: flutter pub get

    - name: Build APK
      run: flutter build apk --release --no-tree-shake-icons

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: afya-full-app
        path: build/app/outputs/flutter-apk/app-release.apk
