name: Afya
description: Healthcare app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # الأساسيات
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.15.8
  # الأدوات
  url_launcher: ^6.2.5
  intl: ^0.19.0
  image_picker: ^1.0.7
  shared_preferences: ^2.2.2 # لحفظ الوضع الليلي
  # الخرائط المجانية (OpenStreetMap)
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  geolocator: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
 
