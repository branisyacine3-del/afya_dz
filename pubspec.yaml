name: afya_dz
description: Afya DZ App - Professional Medical Services.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # مكتبات فايربيز (إصدارات مستقرة)
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  
  # مكتبة لفتح رابط الواتساب والاتصال
  url_launcher: ^6.2.1
  
  # أيقونات عصرية
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # 🛑 لا تضع صوراً هنا (assets) لنتجنب المشاكل حالياً
 
