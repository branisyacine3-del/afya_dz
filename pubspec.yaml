name: Afya
description: Healthcare app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # المكتبات الأساسية
  cupertino_icons: ^1.0.6
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.15.8
  url_launcher: ^6.2.5
  geolocator: ^11.0.0
  intl: ^0.19.0
  image_picker: ^1.0.7
  
  # ✅ الحل السحري: نقلنا مكتبة الأيقونات هنا لكي يراها "GitHub Actions" وتختفي رسالة الخطأ
  flutter_launcher_icons: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

# ==========================================
# 🖼️ إعدادات أيقونة التطبيق (كما طلبت)
# ==========================================
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  # ⚠️ تأكد أن هذا الملف موجود بجانب pubspec.yaml بنفس الاسم تماماً
  image_path: "Picsart_26-01-15_11-27-34-308.png"
  min_sdk_android: 21
