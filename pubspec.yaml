name: afya_dz
description: Healthcare app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: 1.0.6
  firebase_core: 2.24.2
  firebase_auth: 4.15.3
  cloud_firestore: 4.13.6
  # استخدمنا نسخاً محددة بدون علامة ^ لمنع التحديث التلقائي المسبب للمشاكل
  url_launcher: 6.2.5
  geolocator: 11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 3.0.0

flutter:
  uses-material-design: true
